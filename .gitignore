# Rust build output
target/

# Astro output, and the island bundle Astro copies into it. Both are build
# artifacts of two different toolchains; neither is authored.
dist/
public/island/
.astro/

node_modules/

# macOS
.DS_Store

# Local AuthKit configuration. Public values, but per-developer.
.env
.env.local

# Wrangler local dev state.
.wrangler/
