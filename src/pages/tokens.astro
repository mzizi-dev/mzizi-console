---
import Console from "../layouts/Console.astro";
import Island from "../layouts/Island.astro";
---

<Console
  title="Design tokens"
  lede="Seven African minerals. Each carries a light and a dark value and is consumed through its CSS custom property, never as a raw hex."
>
  <Island
    name="tokens"
    fallback="The palette is read from the registry API at runtime, so it needs scripting. See mzizi.dev/tokens instead."
  />
</Console>
